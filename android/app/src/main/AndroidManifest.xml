<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.notesphere.app">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Notesphere"
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config">

        <!-- LAUNCHER -->
        <activity
            android:name=".activities.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.Notesphere.NoActionBar"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- AUTH -->
        <activity
            android:name=".activities.OnboardingActivity"
            android:exported="false"
            android:theme="@style/Theme.Notesphere.NoActionBar"
            android:screenOrientation="portrait" />

        <activity
            android:name=".activities.LoginActivity"
            android:exported="false"
            android:theme="@style/Theme.Notesphere.NoActionBar"
            android:windowSoftInputMode="adjustResize" />

        <activity
            android:name=".activities.RegisterActivity"
            android:exported="false"
            android:theme="@style/Theme.Notesphere.NoActionBar"
            android:windowSoftInputMode="adjustResize" />

        <!-- MAIN -->
        <activity
            android:name=".activities.MainActivity"
            android:exported="false"
            android:theme="@style/Theme.Notesphere.NoActionBar"
            android:launchMode="singleTop"
            android:windowSoftInputMode="adjustResize" />

        <!-- FEATURE ACTIVITIES -->
        <activity
            android:name=".activities.MaterialDetailActivity"
            android:exported="false"
            android:theme="@style/Theme.Notesphere.NoActionBar"
            android:parentActivityName=".activities.MainActivity" />

        <!-- Removed SyllabusDashboardActivity as part of Phase 4 -->        <activity
            android:name=".activities.QuizActivity"
            android:exported="false"
            android:theme="@style/Theme.Notesphere.NoActionBar"
            android:screenOrientation="portrait" />

        <activity
            android:name=".activities.DoubtSolverActivity"
            android:exported="false"
            android:theme="@style/Theme.Notesphere.NoActionBar"
            android:windowSoftInputMode="adjustResize" />

        <activity
            android:name=".activities.SubjectDetailActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.Notesphere.NoActionBar" />

        <activity
            android:name=".activities.NotesListActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.Notesphere.NoActionBar" />

        <activity
            android:name=".activities.NoteEditorActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.Notesphere.NoActionBar" />
        <activity
            android:name=".activities.AccountSettingsActivity"
            android:exported="false"
            android:theme="@style/Theme.Notesphere.NoActionBar"
            android:parentActivityName=".activities.MainActivity" />

        <activity
            android:name=".activities.ShareActivity"
            android:exported="false"
            android:theme="@style/Theme.Notesphere.NoActionBar" />

    </application>

</manifest>
